# Server only. Copy to .env; never prefix this key with VITE_.
# Leave empty to keep local, evidence-based explanations.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Local by default. A hosting provider needs HOST=0.0.0.0 and its assigned PORT.
HOST=127.0.0.1
PORT=4173
